Так и не смог разобраться, каким образом можно через return одновременно передать и объект <class 'float'> и объект <class 'datetime.date'> так, 
чтобы при print(currency_rates('USD')) получить результат 73.7864 2021-03-05.
Если просто передавать два значения получается вот так: (74.4275, datetime.date(2021, 3, 6)). Печатается кортеж, но объект <class 'datetime.date'> выглядит не так как ожидалось.
Ожидаемый вид получается либо при печати результата внутри функции, (там выглядит все как нужно, но, я так понимаю, что через печать вызова функции нужно проверить, 
что функция передает нужные значения?), либо если через return передавать подготовленную строку (что по сути то же самое), что противоречит условиям задачи:
"Функция должна возвращать результат числового типа, например float."
"Дата должна быть в виде объекта date.
Можно переданный кортеж печатать по частям  print(currency_rates('USD')[0], currency_rates('USD')[1]), но такое решение не везде подойдет в этом домашнем задании...
